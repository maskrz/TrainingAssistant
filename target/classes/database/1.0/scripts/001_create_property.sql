CREATE TABLE PROPERTY(
	"NAME" VARCHAR(50) NOT NULL,
	"VALUE" VARCHAR(1000)
);
ALTER TABLE PROPERTY ADD CONSTRAINT PROPERTY_PK PRIMARY KEY ("NAME");
ALTER TABLE PROPERTY ADD CONSTRAINT PROPERTY_NAME_ENUM CHECK ("NAME" IN (
	'WELCOME_TEXT'
));

INSERT INTO PROPERTY VALUES('WELCOME_TEXT', 'Welcome everybody!');